# Shared environment variables for all MCP servers.
# These settings will apply to all services under the /servers directory.
# Non-sensitive defaults only - secrets are defined in secrets/.env.development.local.example

# Currently no shared non-sensitive defaults for all servers.
# Add any shared configuration here as needed.
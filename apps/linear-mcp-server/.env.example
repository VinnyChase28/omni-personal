# =============================================================================
# Linear MCP Server Environment Variables
# =============================================================================
# Non-sensitive service-specific defaults only.
# Secrets like LINEAR_API_KEY are defined in secrets/.env.development.local.example

# Optional: Node.js Environment
NODE_ENV=production

# Optional: Logging Level
LOG_LEVEL=info

# Currently no non-sensitive service-specific defaults for Linear server.
# Add any service-specific configuration here as needed. 
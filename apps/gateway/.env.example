# Environment variables for the MCP Gateway.
# Non-sensitive defaults that can be overridden.

# The port the gateway will listen on
GATEWAY_PORT=37373

# Note: Secrets like JWT_SECRET and MCP_API_KEY are defined in secrets/.env.development.local.example 